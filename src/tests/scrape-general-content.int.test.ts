// TODO: Re-enable once FIRECRAWL_API_KEY is configured.
import { test } from "@jest/globals";
test.skip("Can return markdown from a blog URL", () => {});
test.skip("Can use firecrawl to extract markdown and images", () => {});
