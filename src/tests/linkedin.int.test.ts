// TODO: Re-enable once LinkedIn credentials are configured.
import { describe } from "@jest/globals";
describe("LinkedIn API wrapper", () => {
  // All tests skipped - requires LINKEDIN_CLIENT_ID, LINKEDIN_CLIENT_SECRET, LINKEDIN_ACCESS_TOKEN
});
