# Backend skeleton

Phase 0 directory boundary only. No HTTP server, persistence adapter, auth, or publisher implementation is present.

Subdirectories: `api`, `services`, `repositories`, `publishers`, `audit`, `auth`, `dto`.
